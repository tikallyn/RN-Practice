import React from 'react'
import { Text, StyleSheet } from 'react-native'

const ImageDetail = () => {
    return <Text style={styles.textStyle}>Details!</Text>
}

const styles = StyleSheet.create({
    textStyle: {
        fontSize: 20
    }
})

export default ImageDetail
